using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//TODO:
/*
 * 1>) make this and lines into "measures". e.g. vectorMeasure and measuremanager
 * 2>) Gothrough and remove all warnings.
 * 3>) Stop the master manager from being a singleton
 */
public class PlaneLineManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
